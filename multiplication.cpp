#include <stdio.h>
int main() {
    double a, b, product,exponent,e1,e2;
    printf("Enter two numbers with their exponents: ");
    scanf("%lf %lf", &a, &b);  
 
    
    product = a * b;
    exponent=e1+e2;
    
    printf("Product = %.2lf", product,exponent);
    
    return 0;
}
